# Frontend (set in Render dashboard or .env.local for local dev)
VITE_API_BASE=http://localhost/logbook-api

# Backend (set in Render dashboard — never commit real values)
DB_HOST=your-mysql-host
DB_NAME=siwes_logbook
DB_USER=your-db-user
DB_PASS=your-db-password
JWT_SECRET=change-this-to-a-long-random-secret
